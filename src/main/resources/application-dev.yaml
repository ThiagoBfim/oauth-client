spring:
  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: http://localhost:8080/realms/myrealm
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/myrealm
